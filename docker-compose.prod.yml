services:
  brandcentralaiapp:
    build: 
      context: .
      dockerfile: Dockerfile.prod
      args:
        - ENV=production
    image: brandcentralai:production
    ports:
      - "3000:3000"
    env_file:
      - .env    
    command: npm start
    restart: always 